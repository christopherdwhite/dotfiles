
# | # Command Aliases
# |
# | Quick shortcuts to longer, more complicated or difficult to type commands.
# |

# | # General

alias l="ls -la"       # List in long format, include dotfiles
alias ld="ls -ld */"   # List in long format, only directories
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
